{
  "name": "@burand/pagarme",
  "version": "1.0.0",
  "description": "Fornece métodos essenciais para a manipulação do Pagarme.",
  "scripts": {
    "clean": "rimraf lib",
    "lint": "tsc --noEmit && biome lint ./src",
    "build": "npm run clean && tsc"
  },
  "homepage": "https://github.com/flavianoredressa/burand-functions",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/flavianoredressa/burand-functions.git"
  },
  "files": [
    "lib/"
  ],
  "type": "module",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "dependencies": {
    "@burand/functions": ">=3.0.0",
    "got": ">=14.0.0",
    "tsyringe": ">=4.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "prettier": "^3.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
